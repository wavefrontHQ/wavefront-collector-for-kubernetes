bases:
- ../../versions/

namespace: wavefront-collector

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: collector-config
  behavior: replace
  files:
  - collector.yaml
